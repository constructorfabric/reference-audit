"""reference_audit — audit .bib/.tex references for correctness and hallucination.

The package is a plain async Python application (the `constructor-studio` dependency
provides project governance only, not runtime primitives). See `architecture/` for the
governed design artifacts and `README.md` for the audit specification.
"""

__version__ = "0.1.0"
